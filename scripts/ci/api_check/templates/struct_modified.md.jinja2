
{% for field in fields %}
{{ _generate_('field', field, parent=this) }}
{% endfor %}

{% if desc %}
{{ _notice_ }}
A structure `{{new.name}}` description was changed in `{{new.file}}:{{new.line}}`.

If this is a behavioral change that affects API compatibility, add it to the migration guide.
{% endif %}

{% if file %}
{{ _warning_ }}
A structure `{{new.name}}` was moved from `{{old.file}}:{{old.line}}`
to `{{new.file}}:{{new.line}}`.

If this changes includes in user code, follow the migration procedure and add it
to the migration guide.
{% endif %}
